{% if hojas %}
    <ul>
    {% for hoja_atlas in hojas %}
        <li><a href="/atlas/{{ /polls/{{ poll.id }}/.id }}/">{{ hoja_atlas.nombre }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>Ninguna hoja disponible.</p>
{% endif %}
